Goal: Implement a new medical image descriptor that uses a similarity measure between two images, which is more robust to different types of noise and artifacts.

This project included: determining feature points, clustering, interpolation, training weights, and performing validation.